spring:
  profiles:
    #Profiles: dev, prod, test
    active: prod
  application:
    name: flight-on-time

#Client URL
ds:
  api:
    url: ${DATA_SCIENCE_API_PREDICTION}

#SpringDocs config
springdoc:
  swagger-iu:
    path: ${SWAGGER_IU_CONNECTION:/docs/swagger-ui.html}
  api-docs:
    path: ${SPRING_DOCS_CONNECTION:/docs/api-docs}